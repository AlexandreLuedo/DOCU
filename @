#+title: Emacs Shortcuts
#+OPTIONS:  num:nil
#+SETUPFILE: https://fniessen.github.io/org-html-themes/org/theme-bigblow.setup

* VIM
**   Structuring vim sentences
- *b* + *number* + *motion*
- b = an operator
- motion = like $ w e

** Moving
*This is the motions*
- $ = end of the line
- w = forward word
- e = end of the current word
- 0 = move to start of the line
- G = move to the bottom
- 12G = go to line 12
- gg = start of the page

** Deleting
- x = delete letter
- d = delete
- dd = delete line
- dw = delete word

** Inserting
- i = insert
- a = insert but after one letter
- o = insert below the cursor
- O = insert upper the cursor

** Undo Redo
- u = undo
- U = fix the whole line
- C-r = redo

** Copy, paste & replace
- y = copy/yank
- yy = yank line
- p = for pasting the delted content
- rx = for remplacing the cursor selected letter with the "x"
- ce = change until the end of the line
- cc = the whole line
**   Search
- / = search trhough the doc
- ? = search to the backward position
- n = search the last thing you search
- N = same but in the opposite position
- C-0 = to go back where you are
- % = search the end of an { ( [ ] ) }
**   Modify file type
- s = substitute :s/old/new/g to substitute 'new' for 'old'
** Commands
- :! = type an terminal/external command
- r = retrieving an file (usefull when you make shit)
- R = to remplace than more than one caracter
** Tips
 v + :w nameOfTheFile = you can select an precize part and save it.

* Emacs
[[https://github.com/niyumard/Doom-Emacs-Cheat-Sheet][Ultimate cheatsheet]]

**   Browse internet
- wwe
**      Search internet
M-k
**   Exporting file
- C-c C-e
Remove the table of content when exporting
#+begin_src
#+OPTIONS: toc:nil        (no default TOC at all)
#+end_src
#+begin_src
#+OPTIONS: num:nil
#+end_src

** Open file
*** Local
C-x C-f
*** SSH
C-x C-f
/ssh:user@host:/path/to/file
** Remove & rename file
:delete-file
:rename-file
**  Split and windows manager
*** Split
- C-x 2 or C-x 3 etc...
*** Delete
- C-x 0
***   Move to
- o
** Resize text
C-x + or - or C-+ or C--

** Inserting links
- C-c C-l
** Calculating table formula
:org-table-eval-formula or C-c =
dedicated buffer C-c '
@6$2: ligne6 colonne2
***     ou ajouter en dessous du tableau
#+TBLFM: $2=vsum(@1..@5
*** Mettre à jour automatiquement (recalculer les formules)
C-c * ou M-x org-ctrl-star
** [[https://orgmode.org/worg/org-tutorials/org-spreadsheet-intro.html][Showing table reference (where you are)]]
C-c ?
*** Dedicated buffer
C-c '
**  Journal
-  SPC M-xj
**   Recherche internet
- M-k

- tab M-
** TABS
*** Create
cmd t
*** Switch
- SPC tab 1 / 2 / 3 / etc...
- SPC tab .
*** Delete
- cmd w
- spc tab d
**
** Display time
:display-time-mode
*** Config time
(setq display-time-24hr-format t   ; Affiche l'heure en format 24 heures.
      display-time-day-and-date t) ; Affiche également la date.
(display-time-mode 1)              ; Active l'horloge.
** Display float time
:display-time-world
** Display buffered time
(defun open-clock-buffer ()
  "Open a buffer displaying the current time."
  (interactive)
  (let ((buf (get-buffer-create "*Clock*")))
    (with-current-buffer buf
      (erase-buffer)
      (insert (format-time-string "%A, %d %B %Y\n%H:%M:%S"))
      (goto-char (point-min))
      (display-buffer buf)
      (run-with-timer 1 nil (lambda ()
                              (with-current-buffer "*Clock*"
                                (erase-buffer)
                                (insert (format-time-string "%A, %d %B %Y\n%H:%M:%S"))
                                (goto-char (point-min))))))))
** Link content
- C-l for copying
- C-l C-l for pasting


*   Commands
** Server Attributes
***   Find hostname
#+begin_src bash
hostnamectl
#+end_src
***   Changing server hostname
#+begin_src bash
sudo hostnamectl set-hostname value
#+end_src
**  Show wifi password
#+begin_src
security find-generic-password -ga nom_du_reseau_wifi | grep "password:"
#+end_src

**   Instal *.deb file
#+begin_src
sudo dpkg -i package_file.deb
#+end_src

**  Uninstall & purge any programm
#+begin_src shell
sudo apt-get remove --purge programmName
#+end_src
**  Verify service
#+begin_src
sudo systemctl status name
#+end_src

**  Show what on port
#+begin_src shell
sudo lsof -i -P -n | grep 80
#+end_src
**  MySQL
*** connecting to the server
#+begin_src
mysql -h host -u user -p
#+end_src

    If you are same machine
#+begin_src
mysql -u user -p
    #+end_src
**  Kill an process/app
#+begin_src
pkill -x Emacs
#+end_src
**  Install an .deb file
*** Using dpkg
#+begin_src bash
sudo dpkg -i package_name.deb
#+end_src
**  Convert image
#+begin_src shell
magick image.png image.gif
#+end_src

** Copy a file
cp ancien-fichier.org nouveau-fichier.org
**  Docker
Lister les containers docker
#+begin_src shell
docker ps -a
#+end_src
*   Google
[[https://kinsta.com/blog/google-search-operators/][Google search operator list]]
filetype:pdf
